language: node_js
node_js:
  - '8'
dist: trusty
sudo: required

branches:
  only:
    - master

before_script:
  - yarn global add @angular/cli

script:
  - yarn lint
  - yarn test
  - yarn build --prod --base-href https://github.com/MichaelWarneke/tabularius/
